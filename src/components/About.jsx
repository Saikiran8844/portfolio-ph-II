import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        I'm a passionate <span className="text-[#915EFF]">Software Engineer</span> with strong expertise in 
        <span className="font-semibold"> Java, Spring Boot, and Full-Stack Development</span>.  
        I specialize in building <span className="font-semibold">scalable, secure, and high-performance applications</span> 
        using modern frameworks and cloud technologies.  
        With hands-on experience in <span className="font-semibold">React, Next.js, Kubernetes, and Microservices</span>, 
        I enjoy solving complex challenges and delivering solutions that create real business impact.  

        I'm a quick learner who thrives in collaborative environments and loves 
        <span className="font-semibold"> crafting clean, efficient code</span>.  
        Beyond coding, I’m driven by curiosity and innovation — always eager to explore new technologies 
        and transform ideas into meaningful digital experiences.  
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
